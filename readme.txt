生命游戏c++版
没有ui方面的东东.
一般的都放在配置文件里了.
显示的状态都在标题栏里了

rule_file 指的是规则文件 上面是是一些初始化的图
rule_init 使用第几个规则
front_color back_color line_color 设置颜色
delay 一帧之间的延时

按键:
        L     切换显示线
        Space 暂停/启动
        Left  切换到前一个图
        Right 切换到下一个图
        Esc   结束游戏